# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Data Storage
# The backend reads/writes from ../data/sample-catalog.toon
# No database configuration needed!
